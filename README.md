# Growth Tracker

Supabase-backed growth chart web app.

## Getting Started

1. Copy `.env.local.example` to `.env.local` and fill in your Supabase values.
2. Run `supabase/schema.sql` in the Supabase SQL editor to create tables.
3. Install dependencies and start the dev server.

```bash
npm install
npm run dev
```
